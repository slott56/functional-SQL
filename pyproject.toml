[project]
name = "Functional SQL"
description = "A collection of functions to provide SQL-like processing."
readme = "README.rst"
requires-python = ">=3.12"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.ruff]
line-length = 100
